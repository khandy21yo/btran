Things that need working on.

Clean up #include at start of generated code.
	Make sure includes for boost are Ok.

MAP,RECORD, etc.
	Code is currently buggy.

Built in functions.
	Need to use standard C++ and BOOST versions.

lib routines
	Bring current with btran sources.

String and other library functions
	Use host features where available.
	EDIT$$(CVT$$). Handle boost routines better when multiple bits are set.

Arrays defined like 'DIM X(3 TO 10)' are not handled
	Option didn't exist in the version I based this on.

File IO
	RMS Missing entirely.
	Virtual arrays Currently not working

MAT functions
	Not implemented

