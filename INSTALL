Tools you will need to install

	automake
	autoconf
	libtool
	flex
	bison
	libboost

Configure src

	cd src
	libtoolize
	aclocal
	automake --add-missing
	autoheader
	automake
	autoconf
	./configure
	make

	<error about parse.h>
	cp parse.hh parse.h
	make

Translate code

	btran < test.bas > test.cc

